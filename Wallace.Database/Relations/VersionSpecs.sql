CREATE TABLE [dbo].[VersionSpecs]
(
	[vId] INT NOT NULL FOREIGN KEY REFERENCES Versions(vId) ON DELETE CASCADE,
	[sId] INT NOT NULL FOREIGN KEY REFERENCES Specifications(sId) ON DELETE CASCADE,
	PRIMARY KEY (vId, sId)
)
