CREATE TABLE [dbo].[VersionTeams]
(
	[tId] INT NOT NULL FOREIGN KEY REFERENCES Employees(eId) ON DELETE CASCADE,
	[vId] INT NOT NULL FOREIGN KEY REFERENCES Versions(vId) ON DELETE CASCADE,
	PRIMARY KEY (tId, vId)
)
